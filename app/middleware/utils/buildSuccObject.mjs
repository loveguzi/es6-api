/**
 * Builds success object
 * @param {string} message - success text
 * @returns {Object} success object
 */
const buildSuccObject = (message = '') => ({ msg: message })

export { buildSuccObject }
